# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
python-multipart==0.0.18

# Anthropic
anthropic==0.42.0

# Telephony
twilio==9.4.0

# Speech-to-Text (Hamsa - tryhamsa.com)
# Uses websockets (already in deps) for real-time STT

# Text-to-Speech (Hamsa - tryhamsa.com)
# Uses httpx (already in deps) for API calls

# RAG - Install torch CPU-only first
# Note: Install torch separately before chromadb to avoid GPU version
chromadb==0.5.23
sentence-transformers==3.3.1
pypdf==5.1.0

# Database
motor==3.6.0
pymongo<4.10,>=4.9

# Audio processing (Hamsa outputs mulaw natively, no conversion needed)

# HTTP
httpx==0.28.1
websockets==14.1

# Utilities
structlog==24.4.0
